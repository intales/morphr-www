---
import Layout from '../layouts/Layout.astro';

const codeContent = await Astro.glob('../content/code_snippet.md');

const features = [
  "Design components in Figma with complete freedom",
  "Import them with zero-config setup",
  "Full control over layout and logic",
  "Type-safe property overrides",
];
---

<Layout title="Figflow - From Figma to Flutter">

<section class="pt-24 pb-24">
  <div class="max-w-7xl mx-auto px-8">
    {/* Title */}
    <h1 class="text-center mb-4">
      <span class="text-6xl font-bold block mb-2">
        From Figma to 
        <span class="text-white">Flutter</span>
      </span>
      <span class="gradient-text text-6xl font-bold">Seamlessly.</span>
    </h1>

    {/* Main Section */}
    <div class="mt-24 grid md:grid-cols-2 gap-16 items-start">
      {/* Left Side - Features */}
      <div>
        <h2 class="text-3xl font-bold mb-8">
          Transform designs into Flutter widgets instantly
        </h2>
        
        <p class="text-gray-400 text-xl mb-8">
          Convert your Figma components into pixel-perfect Flutter widgets with complete control over the implementation.
        </p>

        <ul class="space-y-4">
          {features.map(feature => (
            <li class="flex items-start gap-3">
              <svg 
                class="w-6 h-6 text-emerald-500 flex-shrink-0 mt-1" 
                fill="none" 
                stroke="currentColor" 
                viewBox="0 0 24 24"
              >
                <path 
                  stroke-linecap="round" 
                  stroke-linejoin="round" 
                  stroke-width="2" 
                  d="M5 13l4 4L19 7"
                />
              </svg>
              <span class="text-gray-300 text-lg">{feature}</span>
            </li>
          ))}
        </ul>
      </div>

      {/* Right Side - Code Example */}
      <div>
        <div class="bg-gray-900/50 rounded-xl border border-gray-800 p-6">
          <div class="rounded-lg overflow-hidden bg-black/50">
            <div class="p-4">
              <codeContent[0].Content />
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>
</section>

{/* Demo Section */}
<section class="pb-24 relative">
  <div class="max-w-7xl mx-auto px-8">
    <h2 class="text-4xl font-bold text-center mb-16">See it in action</h2>
    <div class="bg-gray-900/50 rounded-xl border border-gray-800 aspect-video">
      <img 
        src="/images/flutter-screen.png" 
        alt="Demo" 
        class="w-full h-full object-cover rounded-xl"
      />
    </div>
  </div>
</section>
</Layout>