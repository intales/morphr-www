---
import Calendly from '../components/Calendly.astro';
import NewsletterSignup from '../components/NewsletterSignup.astro';
import CodeComparison from '../components/CodeComparison.astro';
import Layout from '../layouts/Layout.astro';
---
<Layout title="Morphr - From Figma to Flutter">
<section class="pt-24 pb-16">
  <div class="max-w-7xl mx-auto px-4 sm:px-8">
    {/* Hero Title */}
    <h1 class="text-center mb-12">
      <span class="text-4xl sm:text-6xl font-bold block mb-2">
        From Figma to 
        <span class="text-white">Flutter</span>
      </span>
      <span class="gradient-text text-4xl sm:text-6xl font-bold">Seamlessly.</span>
    </h1>
    {/* Main Value Proposition */}
    <div class="text-center mb-16 max-w-3xl mx-auto">
      <h2 class="text-2xl sm:text-3xl font-bold mb-4">
        Transform designs into Flutter widgets <span class="gradient-text">instantly</span>
      </h2>
      <p class="text-gray-400 text-lg sm:text-xl">
        Convert your Figma components into <span class="gradient-text font-medium">pixel-perfect</span> Flutter widgets with complete control over the implementation.
      </p>
    </div>
    {/* Video Demo Section */}
    <div class="mb-24">
      <div class="bg-gray-900/50 rounded-xl border border-gray-800 overflow-hidden">
        <video 
          autoplay 
          loop 
          muted 
          playsinline
          class="w-full"
        >
          <source src="/assets/video.mp4" type="video/mp4">
        </video>
      </div>
    </div>
    {/* Code Comparison Section */}
    <div class="mb-16">
      <h2 class="text-3xl sm:text-4xl font-bold text-center mb-3">Less code. <span class="gradient-text">More design</span>. Pure magic.</h2>
      <p class="text-center text-gray-400 text-lg mb-12 max-w-2xl mx-auto">See how Morphr transforms complex widget hierarchies<br>into <span class="gradient-text font-medium">clean, maintainable code</span>.</p>
      <CodeComparison />
    </div>
  </div>
</section>
<section class="text-center pb-24">
  <NewsletterSignup />
  <p class="text-sm text-zinc-500 pt-8">
    The first 200 subscribers will get private beta access, a <span class="gradient-text font-medium">lifetime discount</span>, and a direct communication channel with our team to shape the future of morphr.
  </p>
</section>
</Layout>

<style>
  .gradient-text {
    background: linear-gradient(45deg, #FCD34D, #E879F9);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
  }
</style>